# Tensor-Max-Clique
An algorithm to find the maximum clique (equivalent to independent set) of a graph using tensor networks tools.

This is beginning to seriously date. Shows how my code improved over the years.


### Identified immediate needs for refactor

-Split in clean submodules
-Add proper docstring
-Implement JIT compilation
-Unit tests
